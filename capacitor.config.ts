import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.unitconvert.app",
  appName: "unit-convert",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
